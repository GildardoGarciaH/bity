<p id="notice"><%= notice %></p>

<h1>Offices</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Street</th>
      <th>Ext number</th>
      <th>Int number</th>
      <th>Neighborhood</th>
      <th>Postal code</th>
      <th>City</th>
      <th>Country</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @offices.each do |office| %>
      <tr>
        <td><%= office.name %></td>
        <td><%= office.street %></td>
        <td><%= office.ext_number %></td>
        <td><%= office.int_number %></td>
        <td><%= office.neighborhood %></td>
        <td><%= office.postal_code %></td>
        <td><%= office.city %></td>
        <td><%= office.country %></td>
        <td><%= office.user_id %></td>
        <td><%= link_to 'Show', office %></td>
        <td><%= link_to 'Edit', edit_office_path(office) %></td>
        <td><%= link_to 'Destroy', office, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Office', new_office_path %>
